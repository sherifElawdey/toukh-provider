/// Template for [TwilioLocalSecrets]. Copy to `twilio_local_secrets.dart`
/// (or run `bash tool/setup_dart_defines.sh`).
class TwilioLocalSecrets {
  static const accountSid = '';
  static const authToken = '';
  static const serviceSid = '';
}
